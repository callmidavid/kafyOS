#!/usr/bin/env bash
set -eu

state_dir="${XDG_STATE_HOME:-$HOME/.local/state}/kafy"
marker="$state_dir/desktop-initialized"
mkdir -p "$state_dir"

if [ ! -e "$marker" ]; then
  /usr/local/bin/kafy-apply-layout
  touch "$marker"
fi
